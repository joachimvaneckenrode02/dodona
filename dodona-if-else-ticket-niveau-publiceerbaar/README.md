# Ticket kiezen met if, else if en else

Dodona-oefening voor beginnende JavaScript-leerlingen.

Belangrijk:
- `config.json` staat in de hoofdmap van de oefening.
- `evaluation/suite.yaml` bestaat echt.
- In `config.json` staat `"test_suite": "suite.yaml"`.
