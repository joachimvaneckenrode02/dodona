# BMI-zone bepalen met if, else if en else

Dodona-oefening voor JavaScript.

Leerlingen werken met gewone opeenvolgende code, `var`, berekeningen, `if`, `else if`, `else` en `console.log()`.

Er is geen invoer via stdin en er worden geen functies gebruikt.
